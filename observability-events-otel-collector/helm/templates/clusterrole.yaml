# Copyright 2026 The OpenChoreo Authors
# SPDX-License-Identifier: Apache-2.0

{{- if .Values.rbac.create }}
# Cluster-wide read access:
#   - events, namespaces  → required by the k8seventsreceiver
#   - the workload kinds   → required by the k8seventenrich processor's informer
#                            caches (must stay in sync with kinds.go)
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: {{ include "events-collector.clusterScopedName" . }}
  labels:
    {{- include "events-collector.labels" . | nindent 4 }}
rules:
  - apiGroups: [""]
    resources: ["events", "namespaces", "pods", "services"]
    verbs: ["get", "list", "watch"]
  - apiGroups: ["apps"]
    resources: ["deployments", "replicasets", "statefulsets", "daemonsets"]
    verbs: ["get", "list", "watch"]
  - apiGroups: ["batch"]
    resources: ["jobs", "cronjobs"]
    verbs: ["get", "list", "watch"]
{{- end }}
